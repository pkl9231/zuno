const HTTP_RESPONSE_200 = "200";

module.exports = {
  HTTP_RESPONSE_200,
};

// api const

const ZUNO = "zuno";
const QUOTE = "quote";
const FULLQUOTE = "fullquote";
const ISSUE_POLICY = "issue-policy";

exports.module = {
  ZUNO,
  QUOTE,
  FULLQUOTE,
  ISSUE_POLICY,
};
